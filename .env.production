# 生产环境变量（Vite 构建时内联进 bundle）
# 封面主源：同源（Cloudflare Pages 直接 serve dist/covers/，国内可达、稳定）
#   —— jsDelivr→GitHub(独立仓库 mwc-assets) 方案已验证在国内 301 跳 raw 被墙（curl=000），
#      故不采用，mwc-assets 仓库仅作冷备。
# 若需真·CDN 卸载（国内可用），改用 Cloudflare R2 或七牛(中国CDN)，再设 VITE_COVER_BASE 为对应基址。
# VITE_COVER_BASE 留空 => coverUrl() 返回 ./covers/album_<mid>.jpg（同源主源）
VITE_COVER_BASE=
